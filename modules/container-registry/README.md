# Container Registry (ACR) Module

This module deploys an **Azure Container Registry (ACR)** for the platform layer (Repo #2).

It is used by workload repositories (e.g. Repo #3) to **push and pull container images** using Entra ID authentication and RBAC.
